package com.ludogorieSoft.villagelifefrontend.controllers;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.Cookie;
public class LanguageController {

//    @GetMapping("/change-language")
//    public String changeLanguage(@RequestParam("lang") String language, HttpServletResponse response) {
//
//        Cookie cookie = new Cookie("lang", language);
//        cookie.setMaxAge(30 * 24 * 60 * 60);
//        cookie.setPath("/villages");
//        response.addCookie(cookie);
//        System.out.println(" response.addCookie(cookie)  "+ (cookie));
//
//        return "redirect:/";
//    }

}
